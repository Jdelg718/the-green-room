# Synthetic persona validator fixtures

These `.greenroom` archives contain only original synthetic test content. They
are generated by `python -m tests.persona_validator.generate_fixtures` and are
committed so the RED test commit preserves the exact hostile bytes used during
the TDD cycle.

No fixture is extracted or executed by the test suite.

